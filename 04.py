for i in range(1, 11):
    if i % 2 == 0:
        print("Even Number:", i, "Square:", i ** 2)
    else:
        print("Odd Number:", i, "Square:", i ** 2)








if a class orn a object have a double underscore iter the double underscore then is called iterrebel
if a class and object is having both then __ 
a genrator is a special type of interatror which produces value one at a time . 
memory efficent usefull for large data scale value are gentrated only on demand 